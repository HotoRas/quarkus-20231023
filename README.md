# quarkus 프로젝트 시작 (20231023 전도현)
<!--TODO add summary-->
This project uses Quarkus, the Supersonic Subatomic Java Framework.

If you want to learn more about Quarkus, please visit its website: <https://quarkus.io/>.

## Running the application in dev mode

You can run your application in dev mode that enables live coding using:

```shell script
./mvnw quarkus:dev
```

> **_NOTE:_**  Quarkus now ships with a Dev UI, which is available in dev mode only at <http://localhost:8080/q/dev/>.

## Packaging and running the application

The application can be packaged using:

```shell script
./mvnw package
```

It produces the `quarkus-run.jar` file in the `target/quarkus-app/` directory.
Be aware that it’s not an _über-jar_ as the dependencies are copied into the `target/quarkus-app/lib/` directory.

The application is now runnable using `java -jar target/quarkus-app/quarkus-run.jar`.

If you want to build an _über-jar_, execute the following command:

```shell script
./mvnw package -Dquarkus.package.jar.type=uber-jar
```

The application, packaged as an _über-jar_, is now runnable using `java -jar target/*-runner.jar`.

## Creating a native executable

You can create a native executable using:

```shell script
./mvnw package -Dnative
```

Or, if you don't have GraalVM installed, you can run the native executable build in a container using:

```shell script
./mvnw package -Dnative -Dquarkus.native.container-build=true
```
<!-- 
You can then execute your native executable with: `./target/code-with-quarkus-1.0.0-SNAPSHOT-runner`

If you want to learn more about building native executables, please consult <https://quarkus.io/guides/maven-tooling>.

## Related Guides

- REST ([guide](https://quarkus.io/guides/rest)): A Jakarta REST implementation utilizing build time processing and Vert.x. This extension is not compatible with the quarkus-resteasy extension, or any of the extensions that depend on it.
- Qute Web ([guide](https://quarkiverse.github.io/quarkiverse-docs/quarkus-qute-web/dev/index.html)): Serves Qute templates directly over HTTP.
- Qute ([guide](https://quarkus.io/guides/qute)): Offer templating support for web, email, etc in a build time, type-safe way
- OpenID Connect ([guide](https://quarkus.io/guides/security-openid-connect)): Verify Bearer access tokens and authenticate users with Authorization Code Flow
- WebSockets ([guide](https://quarkus.io/guides/websockets)): WebSocket communication channel support -->

<!--
Base generated by Copilot // Co-Authored with Claude Haiku 4.5 (GitHub Copilot)

Prompt:
<code markdown>
이 레포지트리와 커밋 기록을 분석 요약해 주차별 요약을 단일 파일로 정리해줘. 출력은 `/summary.md`로 해줘.
- 주차별 이미지는 3장 정도로서 마크다운 이미지 입력 문법으로 표시하되, 주소는 `/docs/img/`아래에 있는 것으로 가정해. 현재 이미지는 포함돼있지 않고, 이 summary.md를 조정할 때 촬영해 포함 예정이야.
- 줄바꿈은 `<br>\n` 말고 `  \n`으로 처리해.
- 파일 생성은 확장자를 직접 언급하고 있는 것으로 제한하고, PR 생성도 금지.
- 필요하다면 `/docs/temp.md`를 캐시로 활용해도 된다. 근데 아마 필요 없을 거야.
___
`/README.md`를 참조하지 않고 `/summary.md`를 생성해. 나머지 규칙은 종전과 동일.
</code>
-->

---

## 주차별 진행 현황

### 1주차
### 프로젝트 초기화 및 기본 구조

**주요 작업**:
- Initial commit 및 .gitignore 설정
- `/index.html` 레벨 1~4 기본 구조 작성
- CSS 분리: `/index.css` 독립화

![week1-init](/docs/img/week1-init.png)

---

### 2주차
### UI 레이아웃 및 내비게이션 구축

**주요 작업**:
- CSS 파일 분리 및 스타일시트 정리
- 네비게이션 바 추가
- Aatrox 챔피언 모달 구현 (`/#modalAatrox`)
- 푸터 구현
- 페이지 레이아웃 재정렬
- Import 예제 파일 추가

![week2-ui](/docs/img/week2-ui.png)

---

### 3주차
### 프로젝트 문서화 및 마무리

**주요 작업**:
- README.md 작성 및 프로젝트 개요 기록
- 3주차 완료 마크

---

### 4주차
### (진행 사항 없음)

---

### 5주차
### 다운로드 페이지 및 경로 체계화

**주요 작업**:
- `/sub/download.html` 초기 구현 및 추가 컨텐츠 채우기
- `.gitignore` 업데이트
- 챔피언 모달 추가
- 절대경로 기반 리소스 경로 통일

![week5-download](/docs/img/week5-download.png)

---

### 6주차
### JavaScript 기초 및 검색 기능

**주요 작업**:
- 외부 JavaScript 스크립트 구현
- DOM 조작 기초 학습
- 검색 기능 1차 구현 중

---

### 7주차 
### 검색 자동모달 및 네비게이션 동기화

**주요 작업**:
- 검색 기능 추가 개선
- 아트록스 모달을 범용 검색 모달로 리팩토링
- 다운로드 페이지 navbar 동기화
- 후속 수정: CSS 최적화, 검색 자동 모달 수술

---

### 9주차
### 다크 모드 및 데이터베이스 연동

**주요 작업**:
- 다크 모드/라이트 모드 토글러 구현
- MySQL 연동

---

### 10주차
### 로그인 시스템 및 백엔드 기초

**주요 작업**:
- 챔피언 검색, 로그인 페이지 준비
- Entity 클래스 설계
- 로그인 기능 기초 구현

---

### 11주차
### 로그인 기능 확장 및 정리

**주요 작업**:
- 로그인 기능 2차 구현
- 코드 정리 (StartWebSocket.java 파일 삭제)
- SHA256 로그인 해시 기능 추가

---

### 12주차
### 회원가입 수정 및 보안

**주요 작업**:
- 회원가입 기능 수정
- 프로필 조회 기능 추가, 이미지 업로드 기능 추가

---

### 13주차
### Toast 알림 및 프로필 시스템

**주요 작업**:
- 13주차 완료
- showToast 구현 추가

---

### 추가 작업
### DB 마이그레이션 준비

**주요 작업**:
- `/download` CSS 최적화
- 검색 자동모달 수술 (`addEventListener 오류 수정`)
- 챔피언 세부 정보 DB 마이그레이션용 코드 추가
- 

---

## 기술 스택 분석

### 프론트엔드
- **마크업**: HTML5
- **스타일**: CSS3 (모듈화된 파일 구조)
- **스크립팅**: Vanilla JavaScript (외부 파일)
- **UI 요소**: 모달, 내비게이션 바, 토글러, Toast 알림

### 백엔드
- **프레임워크**: Quarkus (Java 기반)
- **데이터베이스**: SQLite / MySQL
- **API**: REST API 구조
- **클래스 구조**: Entity, Resource, Service 계층 분리

### 구현된 주요 기능

| 기능 | 상태 | 주차 |
|---|---|---|
| 기본 UI 레이아웃 | ✅ | 2주 |
| 검색 기능 | ✅ | 7주 |
| 다크/라이트 모드 | ✅ | 9주 |
| 데이터베이스 연동 | ✅ | 9주 |
| 사용자 로그인 | ✅ | 10-11주 |
| 회원가입 | ✅ | 11-12주 |
| Toast 알림 시스템 | ✅ | 13주 |
| 챔피언 상세정보 | 🔄 | 진행중 |

---

## 커밋 분포

- **초기화 (03-11 ~ 03-13)**: 5개 커밋
- **UI 개발 (03-18 ~ 04-01)**: 11개 커밋
- **기능 구현 (04-08 ~ 04-29)**: 8개 커밋
- **로그인/인증 (05-06 ~ 05-13)**: 8개 커밋
- **완성/개선 (05-20 ~ 06-04)**: 7개+ 커밋

---

## 개발 패턴

1. **기능 단위 개발**: 각 주차마다 특정 기능 완성
2. **반복적 개선**: 같은 기능의 재수정 및 최적화 (예: 검색 모달, CSS)
3. **점진적 확장**: UI → 기능 → 데이터베이스 → 사용자 관리 순서로 진행
4. **문서화**: README.md에 진행 상황 기록


## todo! (시간 내 가능 여부 불명)
- 챔피언 총 12체에 대한 모달 구현 (JS상 구현)
  - `document.getElementsByClassName(...).foreach((element)=>{...})`
  - 가능한 경우 JS에 consturctor 삽입 및 전체 DOM 인젝션 구현 가능
- 다운로드 페이지에 검색 코드 인젝션